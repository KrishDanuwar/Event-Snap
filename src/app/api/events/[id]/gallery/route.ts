// src/app/api/events/[id]/gallery/route.ts
// TODO: Phase 5 — GET paginated gallery with signed URLs
import { NextResponse } from 'next/server';

export async function GET() {
  return NextResponse.json({ message: 'Not implemented — Phase 5' }, { status: 501 });
}
