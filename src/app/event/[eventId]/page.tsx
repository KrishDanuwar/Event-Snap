'use client';
// src/app/event/[eventId]/page.tsx
// TODO: Phase 3 — Guest event page with state machine
// States: joining | consenting | camera | gallery | ended | removed
export default function EventPage() {
  return <div>Event Page — Phase 3</div>;
}
